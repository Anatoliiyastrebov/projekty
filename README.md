# Health Questionnaire App (Анкета по здоровью)

Многоязычное веб-приложение для сбора анкет по здоровью с поддержкой DSGVO/GDPR, интеграцией с Telegram и поддержкой трех языков (RU, EN, DE).

## 🚀 Возможности

- ✅ Многоязычный интерфейс (Русский, Английский, Немецкий)
- ✅ DSGVO/GDPR соответствие с политикой конфиденциальности
- ✅ Интеграция с Telegram Bot API для отправки анкет
- ✅ Четыре типа анкет: для младенцев, детей, женщин и мужчин
- ✅ Автосохранение данных в localStorage
- ✅ Валидация форм
- ✅ Адаптивный дизайн

## 📋 Требования

- Node.js 18+ и npm
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Telegram Chat ID (получить у [@userinfobot](https://t.me/userinfobot))

## 🛠️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
cd anketazdoroyou-main
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_TELEGRAM_BOT_TOKEN=your_bot_token_here
VITE_TELEGRAM_CHAT_ID=your_chat_id_here
```

**Важно:** Не коммитьте файл `.env` в репозиторий! Он уже добавлен в `.gitignore`.

### 4. Запуск dev-сервера

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## 📦 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 🌐 Деплой на Netlify

### Настройка переменных окружения на Netlify

**КРИТИЧЕСКИ ВАЖНО:** Переменные окружения в Vite встраиваются в код **во время сборки**. Если вы добавите переменные после сборки, они не будут работать!

#### Пошаговая инструкция:

1. **Перейдите в настройки сайта на Netlify:**
   - Откройте ваш сайт в Netlify
   - Перейдите в **Site settings** → **Environment variables**

2. **Добавьте переменные окружения:**
   - Нажмите **"Add a variable"**
   - **Для первой переменной:**
     - **Key:** `VITE_TELEGRAM_BOT_TOKEN` (только имя, без значения!)
     - **Value:** `7795283302:AAEqvc0ubcVlUJQn4yk70GVVyOGSmrSQKpo`
     - Отметьте **"Contains sensitive value"** (если есть)
     - Нажмите **"Create variable"**
   - **Для второй переменной:**
     - **Key:** `VITE_TELEGRAM_CHAT_ID` (только имя, без значения!)
     - **Value:** `1523168808`
     - Отметьте **"Contains sensitive value"** (если есть)
     - Нажмите **"Create variable"**

3. **ОБЯЗАТЕЛЬНО пересоберите сайт:**
   - Перейдите в раздел **Deploys**
   - Нажмите **"Trigger deploy"** → **"Clear cache and deploy site"**
   - Дождитесь завершения сборки (может занять 2-5 минут)

4. **Проверьте результат:**
   - Откройте сайт в браузере
   - Откройте консоль разработчика (F12)
   - Проверьте, что предупреждение о переменных окружения исчезло
   - Попробуйте отправить тестовую анкету

**Важно:** Если вы видите ошибку "Telegram Bot Token or Chat ID not configured", это означает, что:
- Переменные не были добавлены правильно, ИЛИ
- Сайт не был пересобран после добавления переменных

**Решение:** Убедитесь, что переменные добавлены правильно и пересоберите сайт с очисткой кеша.

## 🔧 Технологии

- **Vite** - сборщик и dev-сервер
- **React** - UI библиотека
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **shadcn/ui** - UI компоненты
- **React Router** - маршрутизация
- **Sonner** - уведомления

## 📝 Структура проекта

```
src/
├── components/        # React компоненты
│   ├── form/         # Компоненты форм
│   └── ui/           # UI компоненты
├── contexts/         # React контексты
├── lib/              # Утилиты и данные
├── pages/            # Страницы приложения
└── hooks/            # Кастомные хуки
```

## 🔐 Безопасность

- Файл `.env` с токенами не должен попадать в репозиторий
- Для продакшена рекомендуется использовать серверный прокси для отправки в Telegram
- Токены Telegram Bot API должны храниться в безопасном месте

## 📄 Лицензия

MIT

## 👤 Автор

Создано с помощью [Lovable](https://lovable.dev)
