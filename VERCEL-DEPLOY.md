# 🚀 Настройка автоматического деплоя на Vercel

## Проблема: Код на Vercel не обновляется автоматически

Если код на GitHub обновляется, но на Vercel ничего не меняется, нужно настроить автоматический деплой.

## ✅ Решение 1: Интеграция Vercel с GitHub (Рекомендуется)

Это самый простой и надежный способ. Vercel автоматически будет деплоить при каждом пуше в GitHub.

### Пошаговая инструкция:

1. **Войдите в Vercel Dashboard:**
   - Откройте https://vercel.com/dashboard
   - Войдите в свой аккаунт

2. **Подключите GitHub репозиторий:**
   - Нажмите "Add New..." → "Project"
   - Выберите "Import Git Repository"
   - Найдите репозиторий: `Anatoliiyastrebov/projekty`
   - Нажмите "Import"

3. **Настройте проект:**
   - **Framework Preset:** Vite
   - **Root Directory:** `./` (корень проекта)
   - **Build Command:** `npm run build` (уже указано в vercel.json)
   - **Output Directory:** `dist` (уже указано в vercel.json)
   - **Install Command:** `npm install`

4. **Добавьте переменные окружения (если нужно):**
   - `VITE_TELEGRAM_BOT_TOKEN` - ваш токен бота
   - `VITE_TELEGRAM_CHAT_ID` - ваш chat ID

5. **Нажмите "Deploy"**

6. **Проверьте настройки Git:**
   - После деплоя перейдите в Settings → Git
   - Убедитесь, что "Production Branch" установлена на `main`
   - Убедитесь, что "Automatic deployments from Git" включено

### После настройки:
- Каждый push в `main` автоматически запустит новый деплой
- Vercel будет показывать статус деплоя в GitHub
- Вы получите уведомления о статусе деплоя

## ✅ Решение 2: GitHub Actions (Альтернатива)

Если интеграция не работает, можно использовать GitHub Actions.

### Настройка:

1. **Получите токены в Vercel:**
   - Перейдите в https://vercel.com/account/tokens
   - Создайте новый токен (Token)
   - Скопируйте токен

2. **Найдите Project ID и Org ID:**
   - Откройте ваш проект в Vercel
   - Перейдите в Settings → General
   - Найдите "Project ID"
   - В URL страницы найдите Org ID (например: `vercel.com/orgs/YOUR_ORG/projects/...`)

3. **Добавьте секреты в GitHub:**
   - Перейдите в ваш репозиторий: https://github.com/Anatoliiyastrebov/projekty
   - Settings → Secrets and variables → Actions
   - Добавьте секреты:
     - `VERCEL_TOKEN` - ваш токен из Vercel
     - `VERCEL_ORG_ID` - ID вашей организации
     - `VERCEL_PROJECT_ID` - ID вашего проекта

4. **Проверьте workflow:**
   - Перейдите в Actions в вашем репозитории
   - Убедитесь, что workflow "Deploy to Production" запускается при пуше

## 🔍 Проверка текущего статуса

### Проверить, подключен ли GitHub к Vercel:
1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в Settings → Git
3. Если видите "Connected to GitHub" - интеграция настроена
4. Если нет - нужно подключить (см. Решение 1)

### Проверить последний деплой:
1. В Vercel Dashboard откройте ваш проект
2. Посмотрите на вкладку "Deployments"
3. Проверьте, когда был последний деплой
4. Если последний деплой старый - значит автоматический деплой не работает

## 🛠️ Ручной деплой (временное решение)

Если нужно срочно обновить код на Vercel:

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Деплой
vercel --prod
```

## 📝 Проверка после настройки

После настройки автоматического деплоя:

1. Сделайте небольшое изменение в коде
2. Закоммитьте и отправьте на GitHub:
   ```bash
   git add .
   git commit -m "Тест автоматического деплоя"
   git push origin main
   ```
3. Проверьте в Vercel Dashboard:
   - Должен появиться новый деплой
   - Статус должен быть "Building" → "Ready"
4. Проверьте сайт - изменения должны появиться

## ⚠️ Важно

- Vercel деплоит только при пуше в подключенную ветку (обычно `main`)
- Убедитесь, что вы пушите в правильную ветку
- Проверьте, что в Vercel настроена правильная ветка в Settings → Git
