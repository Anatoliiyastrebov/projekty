#!/bin/bash

# Скрипт для автоматического коммита и выгрузки всех изменений

set -e

echo "🔄 Автоматическое обновление кода на GitHub..."

# Проверка изменений
if [ -z "$(git status --porcelain)" ]; then
  echo "ℹ️  Нет изменений для коммита"
  exit 0
fi

# Добавление всех изменений
echo "📦 Добавление изменений..."
git add -A

# Автоматическое сообщение коммита с временем
COMMIT_MSG="Автообновление: $(date '+%Y-%m-%d %H:%M:%S')"

# Коммит
echo "💾 Создание коммита..."
git commit -m "$COMMIT_MSG"

# Push (post-commit hook автоматически выполнит push)
echo "✅ Изменения закоммичены. Post-commit hook автоматически отправит их на GitHub..."
