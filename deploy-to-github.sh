#!/bin/bash

# Скрипт для создания репозитория на GitHub и загрузки кода

echo "🚀 Начинаем загрузку на GitHub..."

# Проверяем авторизацию
if ! gh auth status &>/dev/null; then
    echo "⚠️  Требуется авторизация в GitHub"
    echo "📝 Выполняем авторизацию через браузер..."
    gh auth login --web
fi

# Создаем репозиторий
echo "📦 Создаем репозиторий на GitHub..."
gh repo create anketazdoroyou --public --source=. --remote=origin --push

if [ $? -eq 0 ]; then
    echo "✅ Репозиторий успешно создан и код загружен!"
    echo "🔗 URL: https://github.com/$(gh api user --jq .login)/anketazdoroyou"
else
    echo "❌ Ошибка при создании репозитория"
    exit 1
fi

