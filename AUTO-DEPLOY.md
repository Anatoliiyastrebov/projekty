# 🤖 Автоматическая выгрузка на GitHub

Проект настроен с автоматической выгрузкой изменений на GitHub.

## 🚀 Способы автоматической выгрузки

### 1. Автоматический push после коммита (Рекомендуется)

**Настроено автоматически!** При каждом коммите изменения автоматически отправляются на GitHub.

```bash
git add -A
git commit -m "Ваше сообщение"
# Автоматически выполнится: git push origin main
```

### 2. Быстрый авто-коммит всех изменений

Используйте скрипт для автоматического коммита всех изменений:

```bash
./auto-commit.sh
```

Этот скрипт:
- Добавит все изменения
- Создаст коммит с временной меткой
- Автоматически отправит на GitHub (через post-commit hook)

### 3. Отслеживание изменений в реальном времени

Для автоматического коммита при каждом сохранении файла:

```bash
./watch-and-deploy.sh
```

**Требования:**
- macOS: `brew install fswatch`
- Linux: `sudo apt-get install inotify-tools`

### 4. Ручная выгрузка с проверками

Для выгрузки с проверкой кода перед отправкой:

```bash
./push-and-deploy.sh "Описание изменений"
```

## ⚙️ Как это работает

1. **Git Hook (post-commit)**: Автоматически выполняет `git push` после каждого коммита
2. **GitHub Actions**: При пуше в `main` автоматически запускается сборка и деплой
3. **Скрипты**: Упрощают процесс коммита и выгрузки

## 📝 Примеры использования

### Простой способ (рекомендуется):
```bash
# Просто коммитьте изменения - push произойдет автоматически
git add .
git commit -m "Исправления в анкетах"
```

### Автоматический коммит всех изменений:
```bash
./auto-commit.sh
```

### С проверками перед отправкой:
```bash
./push-and-deploy.sh "Добавлены новые вопросы"
```

## ⚠️ Важно

- Все изменения автоматически отправляются в ветку `main`
- GitHub Actions автоматически запускает сборку и деплой
- Убедитесь, что вы работаете в правильной ветке перед коммитом

## 🔧 Отключение автоматического push

Если нужно отключить автоматический push:

```bash
chmod -x .git/hooks/post-commit
```

Для включения обратно:

```bash
chmod +x .git/hooks/post-commit
```
